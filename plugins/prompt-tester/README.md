# prompt-tester

**Run your prompts. Check they work. Catch regressions.**

Executes the `tests.json` generated by prompt-crafter. Each test case is run through the prompt, and output is checked against `expected_contains` assertions. Reports pass/fail per test case with detailed failure reasons.

## Install

Part of the [Wixie](../..) bundle. The simplest install is the `full` meta-plugin, which pulls in all 6 Wixie plugins via dependency resolution:

```
/plugin marketplace add enchanted-plugins/wixie
/plugin install full@wixie
```

To install this plugin on its own: `/plugin install prompt-tester@wixie`. `prompt-tester` has nothing to execute without the `tests.json` that `prompt-crafter` / `prompt-refiner` generate and `convergence-engine` updates — so on its own it has an empty inbox.

## Why

Wixie generates `tests.json` with every prompt, but nothing EXECUTES those tests. This plugin closes the loop — you can now verify a prompt actually works before deploying it, and catch regressions after refinements.

## Usage

```
/test-prompt                          # pick from saved prompts
/test-prompt stocks-analysis          # test a specific prompt
/test-prompt stocks-analysis --ci     # CI mode (JSON output, exit code)
```

## What Happens

```
PROMPT TEST RESULTS: stocks-analysis
Target model: claude-opus-4-6

  PASS  single-stock-output-structure (6/6 assertions)
  PASS  data-labels-present (2/2 assertions)
  FAIL  invalid-ticker-edge-case (0/1 assertions)
        ✗ expected "not recognized" — not found

RESULT: 2/3 passed (67%)
```

## Output

Saves `test-results.json` to the prompt folder with timestamps, pass rates, and per-assertion detail.

## Components

| Type | Name | Model |
|------|------|-------|
| Skill | test-runner | (main agent) |
| Agent | executor | Sonnet — runs individual test cases |

## Behavioral modules

Inherits the [shared behavioral modules](../../shared/) via root [CLAUDE.md](../../CLAUDE.md) — discipline, context, verification, delegation, failure-modes, tool-use, formatting, skill-authoring, hooks, precedent.
